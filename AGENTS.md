# Agent Instructions

- Keep all changes GitHub Pages compatible.
- Prefer editing content files (`_data/*.yml`, `_posts/*.md`, and page Markdown) over changing templates when possible.
- Avoid adding unsupported Jekyll plugins.
- Preserve `relative_url` usage so links keep working with `baseurl`.
